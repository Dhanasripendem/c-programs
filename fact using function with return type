// Online C compiler to run C program online
#include <stdio.h>
int fact(int);

int main()
{
    int  x;
    printf("Enter a number");
    scanf("%d",&x);
    printf("%d",fact(x));
}
int fact(int x)
{
    if(x==0)
    return 1;
    else
    return x*fact(x-1);
}
OUTPUT:Enter a number 7
       5040
