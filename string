#include <stdio.h>

int main() {
    // Write C code here
    char str[100];
    int i;
    printf("ENtera sting");
    gets(str);
    for(i=0;str[i]!='\0';i++)
    {
        if(i%2!=0)
        {
            str[i]=str[i]-32;
        }
    }
    puts(str);
    return 0;
}
